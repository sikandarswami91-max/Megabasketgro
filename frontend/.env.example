# Frontend Environment Configuration
# For local development with backend on port 5001:
VITE_API_URL=http://localhost:5001/api

# When deploying to production (frontend & backend on same origin),
# set this to /api (or leave empty so the default '/api' relative path is used).
